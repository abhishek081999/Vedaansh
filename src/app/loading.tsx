// Soft route transitions keep the AppFramework shell visible.
// A full-screen brand loader here stacked with in-page chart loaders
// (BirthForm / HomeClient) and caused a double-flash on "Open Chart".
export default function Loading() {
  return null
}
